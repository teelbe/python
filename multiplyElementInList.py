# sample list of numbers
numbers = [10, 20, 30, 40, 50]

# initialize a variable to store the product
multiply_of_numbers = 1

# iterate through the list and accumulate the multiply
for i in numbers:
    multiply_of_numbers *= i

# print result
print("Multiply of number in the list:", multiply_of_numbers)
